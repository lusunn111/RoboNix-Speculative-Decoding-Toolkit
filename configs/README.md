# Configuration

The JSON files in this directory are unchanged copies of KERV training configs.
Machine-specific model, dataset, and output paths should be supplied by the
original CLI flags or environment variables; no private `.env` file is copied.
